# Roadmap

- Optional build-tagged Chrome fallback for thin JavaScript shells.
- Slack, Discord, email, and RSS notifiers after webhook demand is proven.
- Screenshot and visual diffs.
- Optional deterministic-summary replacement through the documented summarizer seam.
- PostgreSQL and multi-user mode only if the single-node SQLite model becomes a measured limit.
